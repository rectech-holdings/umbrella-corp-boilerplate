import {useEffect, useState} from "react";

const useCounter = () => {
    const [count, setCount] = useState(0);
    
    useEffect(() => {
        
    });
    const doThing = async () => {
        
    };

    
    return {count, doThing};

};

export default useCounter;