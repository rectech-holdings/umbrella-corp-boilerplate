{
  "name": "ac-admin-api",
  "private": true,
  "main": "./dist/adminApiSDK.js",
  "types": "./dist/adminApiSDK.d.ts",
  "version": "1.0.0",
  "scripts": {
    "dev": "wait-on tcp:5432 && sleep 0.2 && tsc-watch --noClear --onSuccess 'node ./dist/server.js'",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "18.0.3",
    "ac-api": "workspace:*",
    "ac-shared-backend": "workspace:*",
    "ac-shared-universal": "workspace:*",
    "tsc-watch": "5.0.3",
    "typescript": "4.7.4",
    "wait-on": "6.0.1"
  },
  "dependencies": {
    "@fastify/cors": "8.0.0",
    "create-typed-sdk": "workspace:*",
    "fastify": "4.2.0"
  }
}
